in computer terminology , &lt;&lt;unk&gt;&gt; code is code that uses a &lt;&lt;unk&gt;&gt; engine to &lt;&lt;unk&gt;&gt; while keeping the original algorithm intact . that is , the code changes itself each time it runs , but the function of the code ( its &lt;&lt;unk&gt;&gt; ) will not change at all . for example , 1 &lt;&lt;unk&gt;&gt; and &lt;&lt;unk&gt;&gt; both achieve the same result while using different values and operations . this technique is sometimes used by computer viruses , &lt;&lt;unk&gt;&gt; and computer worms to hide their presence . &lt;&lt;unk&gt;&gt; is the most common method to hide code . with &lt;&lt;unk&gt;&gt; , the main body of the code ( also called its &lt;&lt;unk&gt;&gt; ) is &lt;&lt;unk&gt;&gt; and will appear meaningless . for the code to function as before , a &lt;&lt;unk&gt;&gt; function is added to the code . when the code is executed this function reads the &lt;&lt;unk&gt;&gt; and &lt;&lt;unk&gt;&gt; it before executing it in turn . &lt;&lt;unk&gt;&gt; alone is not &lt;&lt;unk&gt;&gt; . to gain &lt;&lt;unk&gt;&gt; behavior , the &lt;&lt;unk&gt;&gt; pair are mutated with each copy of the code . this allows different versions of some code which all function the same .